services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: avatir
      POSTGRES_PASSWORD: avatir
      POSTGRES_DB: avatir
    ports:
      - "5433:5432"
    volumes:
      - avatir_postgres:/var/lib/postgresql/data

volumes:
  avatir_postgres:
