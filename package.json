{
  "name": "avatir",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c cyan,magenta \"npm run dev --workspace @avatir/backend\" \"npm run dev --workspace @avatir/frontend\"",
    "build": "npm run build --workspace @avatir/backend && npm run build --workspace @avatir/frontend",
    "start": "npm run start:prod --workspace @avatir/backend",
    "typecheck": "npm run typecheck --workspaces",
    "db:migrate": "yarn workspace @avatir/backend prisma:migrate:dev"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
